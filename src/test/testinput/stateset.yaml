geometry:
  # Basic geometry configuration for forecast
  nx: 100
  ny: 100
  nz: 50

da_geometry:
  # Data assimilation geometry configuration 
  nx: 50  # Coarser resolution for DA
  ny: 50
  nz: 25

variables: &vars
  variables: [var1]

# Test configuration
test_framework_runtime_config: "--log_level=test_suite"